import React from 'react';
import PropTypes from 'prop-types';

Task.propTypes = {
    
};

function Task(props) {
    return (
        <div>
            
        </div>
    );
}

export default Task;